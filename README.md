# StorageX

StorageX is a JavaFX desktop application for managing users, folders, and stored files.

## Requirements

- Java 17 or newer
- Maven

## Run

From the project root, run:

```bash
mvn clean javafx:run
```

The application uses PostgreSQL for persistence. Configure the database connection and environment values before launching it.
